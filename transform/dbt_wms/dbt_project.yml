name: "dbt_wms"
version: "1.0.0"
config-version: 2

profile: "dbt_wms"

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
docs-paths: ["docs"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_wms:
    +persist_docs:
      relation: true
      columns: true

    staging:
      +materialized: view
      +schema: staging

    marts:
      +materialized: table
      +schema: marts
